{
	people: people/*/filter(name =~ "^B.*" )/*/{name:name,email:email},
	people2: people/*/filter(name =~ "^B.*" )/*/{name:name,email:email}/collate(),
	mapped: people/*/(name =~ "(^B[a-z]+) ([A-Za-z]+)")/{first:.[1],last:.[2]}
}
