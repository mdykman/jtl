{
	people: people/*/filter(name =~ "^B.*" )/*/{name:name,email:email},
	collated: people/*/filter(name =~ "^B.*" )/*/{name:name,email:email}/collate(),
	parsed: people/*/(name =~ "(^B[a-z]+) ([A-Za-z]+)")/filter(.)/{first:.[1],last:.[2]}
}
