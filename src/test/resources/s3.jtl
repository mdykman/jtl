{
	!init: {
      db: module("jdbc",{
         uri:"jdbc:sqlite:src/test/resources/recurse.db",
         driver:"org.sqlite.JDBC"
      })
	},
	_:  wanted/pp(),
	pp: {
		$cnt: count(.),
		_: "select id,name from items where id = ?"/copy(count($_))/join(" UNION ")
	}
}
