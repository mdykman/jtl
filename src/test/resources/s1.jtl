{
	!init: {
      db: module("jdbc",{
         uri:"jdbc:sqlite:src/test/resources/recurse.db",
         driver:"org.sqlite.JDBC"
      })
	},
	_:  {
		a: [2,4,6]/items(),
		b: [2,4,6]/tt(),
		c: [2,4,6]/tu()
	},
	start: ff([$1]),
	ff: $1/items()/amend("children",ff(*/id)),
//	items: . nor db.query(copy("select * from items where parentid = ? ",count())/join(" UNION "), . )
	items: "select * from items where parentid = ? "/copy(count($_))/join(" UNION "),
	tt: .,
	tu: $_
}
